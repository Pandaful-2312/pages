title: Pandaful

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
description: >

theme: minima

header_pages:
  - favorite/favorites.md
  - records/records.md

plugins:
    - jekyll-sitemap
    - jekyll-feed
    - jekyll-remote-theme
    - jekyll-seo-tag

minima:
  skin: auto